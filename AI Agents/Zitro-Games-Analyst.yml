agent_type: "collaborator"
agent_name: "Zitro-Games-Analyst"
model_id: "anthropic.claude-3-5-sonnet-20240620-v1:0"
knowledge_base_ref: "Diccionario de Datos Zitro Games.yaml"

athena_query_rules:
  - "OBLIGATORIO: Consultar 'Diccionario de Datos Zitro Games.yaml' antes de escribir el SQL."
  - "PARTICIONES: Siempre incluir WHERE year = XXXX AND month = X (month como entero 1-12)."
  - "NORMALIZACIÓN: Usar UPPER() en filtros de texto: WHERE UPPER(columna) = UPPER('valor')."
  - "KPIs: Usar fórmulas exactas: Win PUD = SUM(coin_in - coin_out) / SUM(maquinas_dia)."

handshake_out:
  sessionAttributes: 
    gaming_data_processed: true
    kpis_context: "Rendimiento Operativo"

validation_policy: "BLOQUEAR SQL si las columnas no coinciden con el diccionario."